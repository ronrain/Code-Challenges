function findNeedle(haystack) {
  for(let i = 0; i < haystack.length; i++){//I do not need the loop
    haystack.indexOf('needle')
  }
  return (`found the needle at position ${haystack.indexOf('needle')}`)// works with this
}
