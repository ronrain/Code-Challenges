const numbers = [1, 2, 3, 4];
const hasEvenNumber = numbers.some((num) => num % 2 === 0);
// hasEvenNumber is true >>> 2 and 4
//checks if at least one item in the array satifies the provided testing function