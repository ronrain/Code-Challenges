const numbers = [1, 2, 3, 4];
const allPositive = numbers.every((num) => num > 0);
// allPositive is true >>>>> all greater than zero
// checks if all the elemnts in the array satify provided testing function